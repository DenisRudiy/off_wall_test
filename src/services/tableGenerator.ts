export function rowsGeneration(size: number) {
  let rows = [];
  for (let i = 1; i <= size; i++) {
    rows.push(i);
  }
  return rows;
}
export function colsGeneration(size: number) {
  let cols = [];
  for (let i = 1; i <= size; i++) {
    cols.push(i);
  }
  return cols;
}
